<nav class="navbar navbar-default" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">
        <img src="https://dl.dropboxusercontent.com/s/0i7kivoeewnrzeh/tweet-speak-logo.png" alt="">tweet speak english</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
        <li><a href="/dashboard"><span class="glyphicon glyphicon-home"></span> Dashboard</a></li>
        <li><a href="<%= pages_dictionary_path %>"><span class="glyphicon glyphicon-book"></span> Dictionary</a></li>
        <% else %>
            <li><a class="btn btn-link" href="/pages/about">About</a></li>
            <li><a class="btn btn-link" href="/pages/contact">Contact</a></li>
            <li><%= link_to "Sign Up", new_user_registration_path %></li>
            <li><%= link_to "Sign In", new_user_session_path %></li>
        <% end %>
        <% if user_signed_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <% if !current_user.name? %>
                <span class="glyphicon glyphicon-user"></span> <%= current_user.email %><span class="caret"></span></a>
              <% else %>
                <span class="glyphicon glyphicon-user"></span> <%= current_user.name %> <span class="caret"></span></a>
              <% end %>
              
            <ul class="dropdown-menu" role="menu">
              <li><a href="<%= edit_user_registration_path %>"><span class="glyphicon glyphicon-pencil"></span> Edit Profile</li>
              <li><a href="<%= pages_subscription_path %>"><span class="glyphicon glyphicon-credit-card"></span> Manage Subsrcription</a></li>
              <li class= "center bg-info"><%= link_to "Sign Out", destroy_user_session_path, method: :delete %></li>
            </ul>
          </li>
        <% end %>
        <% if current_user.try(:admin?) %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">Admin <span class="caret"></span></a>
            <ul class="dropdown-menu" role="menu">
              <li><%= link_to "Lesson Index", lessons_path %></li>
              <li><%= link_to "New Lesson", new_lesson_path %>
            </ul>
          </li>
        <% end %>
        </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>